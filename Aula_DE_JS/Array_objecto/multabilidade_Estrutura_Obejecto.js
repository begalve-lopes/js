const serie={
    name:'Game of Thrones',
    temporada:8,
    abc:2
}

const obj={
    def:10
}

const frase=`A serie tem ${serie.name} e tem ${serie.temporada+10+serie.abc + obj.def} temporada`
console.log(frase)